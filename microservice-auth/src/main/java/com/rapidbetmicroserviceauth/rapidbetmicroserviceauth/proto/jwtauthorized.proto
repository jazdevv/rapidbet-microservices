syntax = "proto3";
option java_multiple_files = true;

message AuthorizedJWTRequest {
    string token = 1;
}

message AuthorizedJWTResponse {
    bool authorized = 1;
}

service AuthorzedJWTService {
    rpc isAuthorized(AuthorizedJWTRequest) returns(AuthorizedJWTResponse)
}